# Что заменить на боевой контент

Всё, что ниже, — заглушки. Реальные материалы (фото и видео) уже стоят на местах,
подменять нужно только текст, бренд и ссылки.

## 1. Бренд и тексты

| Где | Сейчас | Что нужно |
|-----|--------|-----------|
| `data-split="Резиденция"` (прелоадер и hero) | «Резиденция» | Настоящее название проекта |
| `.hdr__mark`, `.end__foot` | «Резиденция» | То же название |
| `<title>` и `<meta name="description">` | заглушка | Реальные SEO-тексты |
| `.hero__label` | «Клубный дом · 2026» | Реальный статус/срок сдачи |
| `.hero__sub` | «Приватная архитектура…» | Реальный подзаголовок |
| Заголовки секций 01–04 | заглушки | Реальные формулировки |
| `tel:+70000000000` (3 места) | заглушка | Реальный телефон |
| Ссылки `href="#contact"` на кнопках | якорь | Форма заявки / CRM |

## 2. Список инфраструктуры

Вся секция собирается из одного массива в начале `<script>` — добавляйте,
удаляйте и переставляйте пункты прямо там, высота секции пересчитается сама:

```js
const INFRA = [
  {t:'Торговый променад', img:'media/night-street-luxury-shops.jpg',
   d:'Ведущие бренды и авторские рестораны — на первом этаже вашего дома.'},
  ...
];
```

`t` — пункт списка, `img` — кадр слева, `d` — описание под списком.

## 3. Каталог квартир (`flats.html`)

Данные лотов генерируются в начале `<script>` детерминированным генератором —
96 позиций с реалистичным разбросом по этажу, площади, отделке и цене
(цена за м² растёт с этажом, видом на реку и террасой). Под боевые данные
меняется только массив `LOTS`: остальная страница — фильтры, сортировка,
две выдачи, избранное — работает от него.

**Планировки** (`media/plans/*.svg`) нарисованы генератором `genplans.py`:
несущие стены с толщиной, дверные дуги, оконные переплёты, сантехника
и мебель. Это заглушки под реальные чертежи — при замене достаточно
положить SVG с теми же именами (`studio-a`, `one-a`, `two-b`, `pent-a` …).

Что осознанно не сделано: карточка отдельной квартиры, выбор на генплане
и в разрезе корпуса, бронирование, синхронизация фильтров с URL.

## 4. Секция «Планировки» на главной

Содержимое собирается из массива `PL_TYPES` в начале `<script>` в `index.html`:

```js
{k:'2', btn:'2', title:'2 спальни', n:22,
 d:'Избранные конфигурации квартир…',
 f:['two-a','two-b','two-c','two-d']}
```

`btn` — подпись кнопки, `title`/`d` — заголовок и описание панели,
`n` — число квартир для кнопки «Показать N квартир», `f` — список файлов
планировок из `media/plans/`.

## 5. Шахматка (`building.html`)

Дом описан одним массивом в начале `<script>`:

```js
const BLD = [
  {k:1, n:'Корпус 1', sec:[
    {i:1, from:2, to:24, st:['1','2','2','3','2','2','1','s','s','1']},
    ...
  ]}
];
```

`from`/`to` — первый и последний этаж секции, `st` — стояки слева направо
(`s` — студия, `1`/`2`/`3` — комнатность). Пентхаусы в `st` не указываются:
они автоматически ставятся на верхний этаж и занимают по два стояка.
Всё остальное — площади, планировки, цены и статусы — генерируется
детерминированно, поэтому дом одинаков при каждой загрузке.

Под боевые данные меняются две вещи: `BLD` (реальная нарезка дома) и функция
`buildSection` — вместо генерации она должна разбирать выгрузку из CRM.
Базовая цена задана константой `PRICE_BASE` (сейчас 268 000 ₽/м² — цифра
взята с потолка, поставьте свою).

Что осознанно не сделано: сравнение лотов, ссылка на конкретную квартиру в
URL, показ соседей по стояку в карточке, реальная форма бронирования.

## 6. Секция «Сервис»

Карточки собираются из массива `SRV` в начале `<script>` в `index.html`:

```js
{i:'bell', t:'Консьерж 24/7',
 d:'Стойка в лобби работает круглосуточно…'}
```

`i` — ключ иконки из `SRV_ICONS` (иконки нарисованы прямо в коде, тонкой
линией по сетке 48×48), `t` — название, `d` — описание. Номер `01…06`
проставляется автоматически по порядку. Добавляйте и удаляйте пункты —
лента, рейка прогресса и стрелки пересчитаются сами.

Кнопка «Все сервисы» сейчас ведёт на якорь контактов: когда появится
страница сервиса, поменяйте `href` у `.srv__all`.

## 6.1. Новые блоки: генплан, расположение, цифры

**Генплан (`#masterplan`).** Зоны собираются из массива `GP` в начале
`<script>` в `index.html`:

```js
{t:'Двор без машин', m:'0 машин', x:45, y:67,
 p:'39,60 48,55 55,64 52,76 42,77',
 d:'Спортивное ядро, детская зона и тень деревьев…'}
```

`t` — название, `m` — короткая величина в подписи, `x`/`y` — точка подписи,
`p` — контур зоны, `d` — описание в списке справа. Все координаты — проценты
кадра (x слева направо, y сверху вниз), поэтому от размера отрисовки они не
зависят. **Контуры обведены по кадру `media/hero-aerial-sunset-river.jpg`
на глаз** — при замене снимка или появлении настоящего генплана их нужно
переразметить, а деление на «Корпус 1 / Корпус 2» сверить с проектом.
Высотность в подписях (24 и 19 этажей) взята из `BLD` в `building.html`.

**Расположение (`#location`).** Карта — нарисованный SVG прямо в разметке
секции, **стилизация, а не картография**: излучина Сырдарьи, кольцо пешей
доступности и точки-ориентиры расставлены по композиции, а не по координатам.
Значения времени (5 · 10 · 15 · 25 мин.) правдоподобны для Худжанда
(аэропорт — около 10 км от центра, Кайраккумское водохранилище — около 20 км),
но **привязаны к центру города, а не к точному адресу проекта** — под боевые
данные их нужно пересчитать от реальной точки, вместе с подписями под цифрами.

**Проект в цифрах (`#numbers`).** Значения выведены из `BLD` в
`building.html`: 3 корпуса, 9 секций, 24 этажа в доминанте, 1 070 квартир
(первый этаж — коммерция, верхний — пентхаусы по два стояка). Если меняется
`BLD`, цифры в секции нужно пересчитать.

**Фото в новых блоках.** «Панорама» и «Атмосфера» используют один и тот же
`media/aerial-river-daytime.jpg`; когда появится кадр вида из окна на реку,
в «Панораме» его стоит заменить — блок написан именно про этот вид.

## 7. Медиа

Все пути собраны в `media/`. Замена — один в один по имени файла.

| Файл | Где используется |
|------|------------------|
| `hero.mp4` / `hero.webm` | Фон hero (играет один раз, не зациклен) |
| `hero-poster.jpg` | Постер видео (первый кадр) |
| `facade-detail-balconies.jpg` | Сцена «раскрытие рамки» |
| `facade-day-parklane.jpg` | Глава 01 |
| `courtyard-gazebo-lifestyle.jpg` | Глава 02 |
| `aerial-river-daytime.jpg` | Глава 03 и пункт «Набережная и парк» |
| `night-street-luxury-shops.jpg` | Пункт «Торговый променад» |
| `courtyard-sports-aerial.jpg` | Пункт «Спорт и площадки» |
| `courtyard-playground-aerial.jpg` | Пункт «Детская среда» |
| `rooftop-terrace-aerial.jpg` | Пункт «Террасы на кровле» |
| `aerial-night-river.jpg` | Пункт «Вечерний свет» |
| `facade-day-hero-a.jpg` | Пункт «Авторские лобби» |
| `night-elevation-dusk.jpg` | Финальный блок |

**Не задействован:** `hero-aerial-sunset-river.jpg`.

Если меняете hero-видео: чтобы луп остался бесшовным, пересоберите его
палиндромом (команда — в конце файла).

## 8. Шрифты

Сейчас грузятся Google Fonts: **Manrope** (текст, интерфейс), **Unbounded**
(вордмарк, крупные цифры) и **Prata** (засечка, только секция «Сервис»). Это подбор «по духу» — оригинальные CoFoSans /
CoFoRobert платные (Colophon Foundry). Меняются двумя переменными в начале
`<style>`: `--f-sans` и `--f-disp` (плюс строка `<link>` с Google Fonts).

Если Manrope не понравится, вот кандидаты с хорошей кириллицей:
`Golos Text` (нейтральный, «редакционный»), `Onest`, `Geologica`
(характерный, чуть уже), `Jost` (геометричный, futura-подобный — сильно
«архитектурный»). Вес заголовков задан в `.h-xl/.h-lg/.h-md` — если хочется
жёстче, поднимите `font-weight` с 400 до 500.

Вордмарк вписывается в колонку измерением (`fitMark()`), поэтому при
смене шрифта он не «вылезет» за границы — размер подстроится сам.

## 9. Ещё не сделано (осознанно, вне текущего объёма)

- Форма заявки — сейчас кнопка ведёт на якорь, формы нет
- Мобильное меню-гамбургер — на узких экранах в шапке остаётся только CTA,
  навигация живёт в плавающей пилюле
- Блок «Локация» с цифрами убран: в прошлой версии он выглядел слабо.
  Если нужен — вернём отдельной сценой
- Модалка с видео по кнопке «Смотреть фильм о проекте»
- Разделы «Планировки», «Отделка», «Ход строительства», 3D/2D-просмотр,
  бронирование — это отдельные страницы, здесь только главная

## 10. Пересборка hero-видео (грейд + апскейл)

Луп больше не нужен: ролик играет один раз и замирает на последнем кадре.

```bash
FC="scale=1280:696:flags=lanczos,unsharp=5:5:0.4:5:5:0.0,\
eq=contrast=1.06:brightness=-0.030:saturation=0.82,\
colorbalance=rs=-0.020:bs=0.030:rh=0.025:bh=-0.018,format=yuv420p"

ffmpeg -i source.mp4 -map 0:v:0 -an -vf "$FC" \
  -c:v libx264 -crf 29 -preset slow -pix_fmt yuv420p -movflags +faststart hero.mp4

ffmpeg -i source.mp4 -map 0:v:0 -an -vf "$FC" \
  -c:v libvpx-vp9 -crf 42 -b:v 0 -row-mt 1 hero.webm

# постер — первый кадр с тем же грейдом
ffmpeg -i source.mp4 -map 0:v:0 -an -vf "$FC" -frames:v 1 -q:v 3 hero-poster.jpg
```

`-map 0:v:0` обязателен: в исходнике часто лежит обложка отдельным
mjpeg-потоком, без этого ffmpeg возьмёт её вместо видео. `-an` снимает
звук — он в фоне не нужен и весит.

Исходник `media/hero_video.mp4` в раздаче не участвует: его можно унести
из `site/media/` к остальным рендерам, чтобы не таскать лишние 5 МБ.
